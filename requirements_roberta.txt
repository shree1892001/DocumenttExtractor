# Requirements for RoBERTa-based Confidential Document Processing
# Install with: pip install -r requirements_roberta.txt

# Core ML libraries
transformers>=4.21.0
torch>=1.12.0
tokenizers>=0.13.0

# Image processing and OCR
opencv-python>=4.6.0
numpy>=1.21.0
Pillow>=9.0.0
pytesseract>=0.3.10

# Document processing
PyMuPDF>=1.23.0  # For PDF processing
python-docx>=0.8.11  # For DOCX processing
pdf2image>=1.16.0  # For scanned PDF processing

# Text processing
regex>=2022.7.9

# System dependencies (install separately)
# For pdf2image on different systems:
# Windows: Download and install poppler-utils
# macOS: brew install poppler
# Ubuntu/Debian: sudo apt-get install poppler-utils
# CentOS/RHEL: sudo yum install poppler-utils

# For pytesseract OCR:
# Windows: Download and install Tesseract-OCR
# macOS: brew install tesseract
# Ubuntu/Debian: sudo apt-get install tesseract-ocr
# CentOS/RHEL: sudo yum install tesseract

# Optional: For GPU acceleration (if available)
# torch-audio  # Uncomment if you have CUDA-capable GPU
# torchvision  # Uncomment if you have CUDA-capable GPU

# Existing dependencies (should already be installed)
# google-generativeai  # Only used for non-confidential documents
